env:
  global:
    - REPOSITORY="amontalban/deb-builder"
    - CI_HOME=`pwd`/$REPOSITORY
language: python
python:
 - "2.7"
before_install:
  - gem install package_cloud
script:
  - bash env.sh
  - sudo -E sh -c "bash init.sh"
  - sudo -E sh -c "bash build.sh"
  - bash push.sh
